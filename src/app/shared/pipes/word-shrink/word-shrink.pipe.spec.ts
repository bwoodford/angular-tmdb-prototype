import { WordShrinkPipe } from './word-shrink.pipe';

describe('WordShrinkPipe', () => {
  it('create an instance', () => {
    const pipe = new WordShrinkPipe();
    expect(pipe).toBeTruthy();
  });
});
